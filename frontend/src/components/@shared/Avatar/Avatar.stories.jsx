import Avatar from '.';

export default {
  title: 'Components/Avatar',
  component: Avatar,
};

const Template = (args) => {
  return <Avatar {...args} />;
};

export const Default = Template.bind({});
