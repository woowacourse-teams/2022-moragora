import styled from '@emotion/styled';

export const LogoBox = styled.div`
  display: flex;
  align-items: center;
`;

export const Title = styled.h1`
  margin: 0;
  font-size: 48px;
  font-weight: 700;
`;
