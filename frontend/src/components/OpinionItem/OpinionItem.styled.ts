import styled from '@emotion/styled';

export const Layout = styled.div`
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  padding: 0.75rem;
`;

export const nameSpan = styled.span`
  font-size: 1rem;
`;

export const Box = styled.div`
  display: flex;
`;

export const TimestampParagraph = styled.p`
  margin: 0;
  margin-left: 1rem;
`;

export const OpinionParagraph = styled.p`
  margin: 0;
  display: block;
  word-break: break-all;
`;

export const UpdatedSpan = styled.span`
  margin-left: 1rem;
  color: gray;
`;
