import MobileLayout from '.';

export default {
  title: 'Components/MobileLayout',
  component: MobileLayout,
};

const Template = (args) => {
  return <MobileLayout {...args} />;
};

export const Default = Template.bind({});
