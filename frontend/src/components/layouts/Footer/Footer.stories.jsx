import Footer from '.';

export default {
  title: 'Components/Footer',
  component: Footer,
};

const Template = (args) => {
  return <Footer {...args} />;
};

export const Default = Template.bind({});
